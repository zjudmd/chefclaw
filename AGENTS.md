# Workflow Notes

- Keep `README.md` up to date whenever the public API, request/response contract, setup steps, recipe management flow, or user-visible behavior changes.
- Before finishing a feature that changes external behavior, verify that `README.md` still matches the current implementation.
